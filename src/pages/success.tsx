import { FC } from "react";

const Success: FC = () => {
  return (
    <div className="flex flex-col">
      <h1 className="text-5xl">Checkout Successful</h1>
      <h2 className="text-3xl">Your order is on the way!</h2>
    </div>
  );
};

export default Success;
